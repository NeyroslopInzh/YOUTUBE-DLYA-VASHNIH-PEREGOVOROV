# Linux-бинарник

Собирается на Linux-машине:

```bash
chmod +x ../../build_linux.sh
../../build_linux.sh
```

Результат: `youtube-clipper` — запуск `./youtube-clipper`

На Windows этот бинарник не собирается (нужна Linux-система с Python 3.10+).
