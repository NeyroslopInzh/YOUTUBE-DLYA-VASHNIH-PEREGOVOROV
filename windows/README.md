# Windows

- `run.bat` — запуск из исходников
- `build.bat` — сборка exe в `dist/windows/`
- `install.bat` — копия exe в `%LOCALAPPDATA%\YVPClipper\` + регистрация `yvp://` (для расширения)
- `app.spec` — PyInstaller (включает ffmpeg через imageio-ffmpeg)
